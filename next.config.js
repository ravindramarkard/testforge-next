/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Allow the pages/ directory alongside src/app/ (hybrid mode)
  // Next.js 14 supports both App Router and Pages Router simultaneously
}

module.exports = nextConfig
